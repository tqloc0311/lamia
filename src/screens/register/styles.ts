import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  absoluteFill: {
    position: 'absolute',
    top: 0,
    bottom: 0,
    left: 0,
    right: 0,
  },
  title: {
    fontSize: 16,
    fontWeight: '600',
    marginBottom: 12,
  },
  input: {},
  registerButton: {
    alignSelf: 'flex-end',
  },
});

export default styles;
